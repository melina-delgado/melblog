baseURL: 'https://example.org/'
languageCode: 'en-us'
title: 'MelBlog'
theme: 'stack'

DefaultContentLanguage: en

params:
  sidebar:
      avatar:
          enabled: true
          local: true
          src: img/avatar.png
  widgets:
    homepage:
        - type: search
        - type: archives
          params:
            limit: 5
        - type: categories
          params:
            limit: 10
        - type: tag-cloud
          params:
            limit: 10
    page:
        - type: toc
